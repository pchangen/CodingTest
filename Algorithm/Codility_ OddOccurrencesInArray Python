OddOccurrencesInArray
Python

// pop 하는 것 보다 i 증가 시키는게 빠른듯

  
def solution(A):
    # write your code in Python 3.6
    A.sort()
    i = 0
    while(1):
        if(len(A)-1 == i):
            return A[i]
        if(A[i] != A[i+1]):
            return A[i]
        i += 2
    pass
